import { Upload, X } from 'lucide-react';
export function ImportModal({ result, onClose, onSubmit }: { result:any; onClose:()=>void; onSubmit:(e:React.FormEvent<HTMLFormElement>)=>void }) { return <div className="modalveil" onClick={onClose}><div className="importbox" onClick={e=>e.stopPropagation()}><button className="x" onClick={onClose}><X/></button><Upload size={28}/><h3>Importer une situation Op@le</h3><p>Balance, budget, mouvements du 5151, CLCA ou FDR. Vigie détecte la source et recalcule le cockpit.</p><form onSubmit={onSubmit}><input name="file" type="file" accept=".lis,.csv" required/><button className="btn-primary-v">Analyser et importer</button></form>{result&&<div className="result">{result.loading?'Analyse en cours…':result.error?`Import refusé : ${result.error}`:`${result.type} · ${result.control?.importedRows||0} lignes importées`}</div>}</div></div> }
